.DS_Store
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
private/
.replit
replit.nix

# content/ is a local symlink to the developer's Obsidian vault — never commit
content

# macOS app bundle output
dist/
